apiVersion: v1
kind: Service
metadata:
  name: backend-service 
  namespace: production
  labels:
    environment: production-app
    app: food-app
    developer: suriyaprashad
spec:
  type: ClusterIP
  selector:
    app: node-app
  ports:
  - name: backend
    port: 4000
    targetPort: 4000
    protocol: TCP